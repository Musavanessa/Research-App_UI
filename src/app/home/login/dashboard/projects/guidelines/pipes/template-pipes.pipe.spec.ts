import { TemplatePipesPipe } from './template-pipes.pipe';

describe('TemplatePipesPipe', () => {
  it('create an instance', () => {
    const pipe = new TemplatePipesPipe();
    expect(pipe).toBeTruthy();
  });
});
